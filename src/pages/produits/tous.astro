---
import Layout from '~/layouts/Layout.astro';
import SelectionPage from '~/components/catalog/SelectionPage.astro';
import QuoteCta from '~/components/home/QuoteCta.astro';
import { getMixedProducts } from '~/content/queries';
import { getAllProductsUrl } from '~/utils/catalog';
import { ui } from '~/i18n/ui';

const products = await getMixedProducts();
---

<Layout title={`${ui.fr['allProducts.title']} — ${ui.fr['site.title']}`} description={ui.fr['allProducts.intro']}>
	<main class="mx-auto max-w-6xl px-4 py-10">
		<SelectionPage
			lang="fr"
			title={ui.fr['allProducts.title']}
			intro={ui.fr['allProducts.intro']}
			products={products}
			payloadUrl={`${getAllProductsUrl('fr')}/listing.json`}
		/>
	</main>
	<QuoteCta lang="fr" />
</Layout>
